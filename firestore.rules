
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return get(/databases/$(database)/documents/user/$(request.auth.uid)).data.role == 'admin';
    }

    match /user/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId || isAdmin();
      
      // Aturan untuk sub-koleksi alamat
      match /addresses/{addressId} {
        allow read, write: if request.auth.uid == userId;
      }
    }

    match /products/{productId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }
    
    match /stock_adjustments/{adjustmentId} {
      allow read, write: if isAdmin();
    }

    match /purchase_transactions/{transactionId} {
      allow read, write: if isAdmin();
    }
    
    match /operational_expenses/{expenseId} {
        allow read, write: if isAdmin();
    }

    match /orders/{orderId} {
      // Admin bisa membaca/menulis semua pesanan
      // Reseller bisa membuat pesanan baru (create)
      // Reseller bisa membaca pesanan mereka sendiri (get, list)
      allow create: if request.auth.uid == request.resource.data.customerId;
      allow read, update: if isAdmin() || request.auth.uid == resource.data.customerId;
      allow delete: if isAdmin();
    }
    
    match /bank_accounts/{accountId} {
        allow read: if request.auth != null;
        allow write: if isAdmin();
    }
    
    match /whatsapp_contacts/{contactId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }

  }
}
